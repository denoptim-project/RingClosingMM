# Plotting of RCP-related Energy Profiles

This is a collection of scripts used to plot the energy terms in various systems to evaluate whether the energy profile was indeed as expected.

### Prerequisites

The package must be installed. For development, install in editable mode within a conda environment. For regular use, install via conda: `conda install ringclosingmm`.

### Usage

```bash
python <script_name.py>
```
